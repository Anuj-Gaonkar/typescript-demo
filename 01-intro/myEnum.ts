enum seatChoice {
    AISLE = "aisle",
    MIDDLE = 0,
    WINDOW = 1
}

let seat = seatChoice.AISLE;

console.log(seatChoice.MIDDLE);

const enum seatChoice1 {
    AISLE = "aisle",
    MIDDLE = 0,
    WINDOW = 1
}

const seat1 = seatChoice1.AISLE;

console.log(seatChoice1.MIDDLE);
